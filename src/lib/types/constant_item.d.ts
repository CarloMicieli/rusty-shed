// Minimal type declarations for railway constants used across the app.
// Add more fields if other files require them.
export type ConstantItem = {
  id: string;
  display?: string;
  labelKey?: string;
  // ...other fields may exist in real data
};
