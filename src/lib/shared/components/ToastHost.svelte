<script lang="ts">
  /**
   * Toast Host Component (Shared)
   * Renders the toast notification provider for shadcn-svelte
   * Displays all active toasts in top-right corner
   */
  import ToastProvider from '$lib/components/ToastProvider.svelte';
</script>

<ToastProvider />
