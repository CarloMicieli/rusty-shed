/// Media storage interface
#[derive(Debug)]
pub struct MediaStorage;

impl MediaStorage {
    /// Placeholder for media storage
    pub fn new() -> Self {
        Self
    }
}

impl Default for MediaStorage {
    fn default() -> Self {
        Self::new()
    }
}
